# Modus：架构宪法

> 这不是设计文档。这是设计原则的起源。每次偏离方向时，回到这里重新确认。

---

## 零、文档状态

本文第一至第七部分保留项目的哲学愿景。除非出现在下方"当前已实现合同"中，否则不得把愿景描述当成已经存在的安全或可靠性能力。

### 当前已实现合同（P0/P1）

1. **统一 run 身份**：所有模式共享 `run_id`、`RunController`、`RunBudget` 和 typed event 序列。
2. **非阻塞与互斥**：模型任务在 session 后台 task 中执行；同 session 第二个 run 明确拒绝。
3. **终态真实**：成功只有 `completed`；预算、取消、错误都有结构化失败终因。
4. **取消传播**：取消向所有模式的底层 awaitable 传播。
5. **审批单次消费**：审批绑定 `(run_id, approval_id, input_hash)`，fail closed。
6. **可恢复账本**：SQLite 持久化 runs、run_events、approvals；启动时 running → interrupted。
7. **只读 Worker**：Peri Worker 只能读、搜索、访问允许的 web/skill 工具。
8. **凭据边界**：浏览器 DTO 不回传模型/MCP secret；审计 payload 递归脱敏。
9. **安全压缩**：只有模型上下文会压缩；SQLite message log 不被替换。
10. **单一会话身份**：WebSocket session 与 SQLite `db_id` 明确分层。
11. **模型发现与能力来源**：厂商可用性与能力字段分离，来源明确标注。
12. **Artifact-first 交换**：Peri Worker 从 SHA-256 校验的 artifact 读取。
13. **可写 Worker（双审批门）**：`features.writable_workers` 开启后，Worker 在私有 worktree 内写文件并 git add/commit；worktree 创建与合并回主分支分别经 `create_worktrees`/`merge_changes` 审批门，不 push、不自动合并、不强制清理。
14. **显式 WorkspaceIdentity**：Session 与 Run 绑定稳定的 `workspace_id/root/name`。
15. **统一工作台实体**：每个 Run 有一个持久化根 Task；MOA Reference 与 Peri Worker 是其子 Task。
16. **展示与状态分离**：时间线负责叙事，工作台负责权威状态。
17. **可恢复运行审阅**：工作台可按当前会话切换历史 Run。
18. **收敛检测（SPRT + 语义坍缩）**：Peri 审阅循环有界多轮，`decide()` 用 Host 审阅分数做 SPRT、用 n-gram 语义重叠判坍缩；收敛则接受当前共识并正常完成，无进展达轮数上限则失败。
19. **结构化 success_criteria 验证**：Host 满意从主观 0-10 分改为**逐条布尔判定**——每个子任务的 `success_criteria` 是结构化 checklist，Host 逐条判定"满足/不满足"+ 理由；SPRT 收敛信号用"已满足 criteria 数"而非主观分数，criteria 是权威（全部满足即达标，即使整体审阅不认可）。
20. **Shell 资源限制**：`sandbox.enabled` 开启后，bash/run_tests 子进程经 `preexec_fn` 施加 RLIMIT_CPU/RLIMIT_FSIZE/RLIMIT_NOFILE，CPU 秒、输出大小、文件描述符被硬性约束；超限有可读错误（CPU limit / size limit exceeded）。默认关闭，不改变现有行为。
21. **Run 级 park/resume**：`features.park_on_disconnect` 开启后，WebSocket 断线不再取消 run，而是 detach 传输（PAUSED 态）、run 继续执行、事件仍持久化到 SQLite；重连后新 socket 经 `resume_session` 绑定到 park 的 run，从断点续收。默认关闭，断线仍 fail-closed 取消。
22. **递归可写子 Agent**：`convergence.max_recursion_depth` > 0 时，Peri worker 获得 `spawn_subtask` 工具，可把自己的范围拆给子 worker（深度受限、`_subtasks/` 子目录隔离、共享 run budget、`parent_task_id` 嵌套记账）。默认关闭（depth=0）。
23. **Keychain 凭据后端**：macOS 上经显式批准的 Keychain 后端把密钥移入系统钥匙串，JSON 只留存储标记；迁移前显示脱敏报告（仅尾缀）、需显式确认、写时间戳备份。
24. **Reasoner 抽象**：推理策略可插拔——`ReActReasoner` 是默认策略（事件词汇 `text_delta/tool_call/done` 等不变），未来 AGI 通过 `QueryEngine.ask(reasoner_factory=...)` 换策略而不重写 runner/预算/审批/持久化。配套 `ContextProvider`（上下文组装抽象）与 `Capability`（模型能力协商）为 AGI 预留接缝。
25. **AGI mode 预留**：`modes.py` 注册 `agi` mode（含前端入口），`task_kind` 放开闭集——未来 AGI 可注册新任务种类与运行模式而无需改协议或 schema。

### 尚未实现，不构成承诺

- shell 文件系统隔离（资源限制 RLIMIT 已实现；chroot/sandbox 需要 root 或已废弃的 sandbox-exec，未实现）。
- Linux Secret Service 凭据后端（macOS Keychain 已实现，见合同 23）。
- Provider 流级续传：DeepSeek/OpenAI-compatible 无原生 resume token，真正的流级续传需 provider 支持（run 级 park/resume 已实现，见合同 21）。

---

## 一、哲学公理

### 公理 1：道，不是角色
主 Agent 是用户意图进入系统的唯一入口。面对每个任务，它**自行生成**世界观。**不能预设主 Agent 的 role。**

### 公理 2：阴-阳-合是基本运动形式
LLM↔LLM 循环是辩证的负熵循环：主 Agent 提出世界观（正），子 Agent 实践并发现矛盾（反），双方碰撞产生新世界观（合）。**每一次循环必须改变至少一方的状态。**

### 公理 3：矛盾不会消失，只会转移层级
循环终止的条件是"当前层级的所有可识别矛盾已消解到表达性层面"。**检测「实质性分歧 vs 表达性分歧」是系统的核心能力。**

### 公理 4：子 Agent 不从用户那里直接读取意图
用户只和主 Agent 对话。子 Agent 只理解主 Agent 翻译后的世界观片段。**用户微操通过主 Agent 中转。**

### 公理 5：系统是开放的
用户可以在任何时候注入新输入，打破当前的任何均衡。**没有一个循环周期是神圣不可打断的。**

---

## 二、三种认知形态的哲学与经济学定义

### 2.1 Default（含 Subagent）：层级星系

- **宇宙隐喻**：一个中心恒星指挥着围绕它的行星。
- **拓扑**：星型。
- **核心目的**：效率。
- **对多样性的态度**：不关心。
- **成本模型**：线性 O(n)。适合直接使用高价格前沿模型。
- **适用边界**：绝大多数任务。

### 2.2 MOA：平行透镜宇宙

- **宇宙隐喻**：多个引力透镜同时观测同一个天体，光线在聚合器处干涉。
- **拓扑**：并行。
- **核心目的**：质量。
- **对多样性的态度**：直接利用。
- **成本模型**：指数 O(k×n)。不推荐使用高价格前沿模型。
- **适用边界**：质量要求极高、成本预算充足的场景。

### 2.3 Peri：分形生长宇宙

- **宇宙隐喻**：奇点裂解为多个局域因果锥，通过观测和负熵审阅收敛为结构。
- **拓扑**：迭代循环。
- **核心目的**：深度。
- **对多样性的态度**：辩证综合。
- **成本模型**：非线性，可控。总成本 ≈ 主模型 ×（1+α），α < 1。
- **工程前置条件**：任务拆解、长连接通信、上下文管理、迭代终止判定。
- **适用边界**：需要多轮调研、审阅和修订的复杂项目。

### 2.4 三者完整对比

| 维度 | Default | MOA | Peri |
| :--- | :--- | :--- | :--- |
| 拓扑 | 星型 | 并行 | 迭代循环 |
| 目的 | 效率 | 质量 | 深度 |
| 多样性态度 | 不关心 | 直接利用 | 辩证综合 |
| 成本 | O(n) | O(k×n) | 主模型×(1+α)，α<1 |
| 前沿模型适配 | ✅ | ❌ | ⚠️ |

---

## 三、架构结构

> **本章归属指引**：3.1（认知调控维度）是 **Peri 专属架构**；3.2（参与者边界）是 **通用架构**，但"上报分歧"是 Peri 特有行为；3.3（终止条件）中，层 0 和层 3 是 **通用**，层 1 和层 2 是 **Peri 专属**（已实现）。

### 3.1 认知调控维度（Peri 专属）

以下两个维度是 **Peri 模式的专属架构**。MOA 不涉及迭代收敛与矛盾路由（它是一次性的并行聚合），Default 更不涉及。Peri 通过这两个维度实现其"辩证迭代"的认知循环。

**维度 A：收敛检测**

| 层级 | 定义 | 归属 |
| :--- | :--- | :--- |
| L0 | Token/轮次硬预算 | 共享基础 |
| L1 | SPRT 统计决策（基于 criteria 满足数变化） | Peri 专属（已实现） |
| L2 | 语义坍缩检测 | Peri 专属（已实现） |

**维度 B：矛盾路由**

| 层级 | 定义 | 归属 |
| :--- | :--- | :--- |
| L0 | 当前世界观内优化 | Peri 核心 |
| L1 | 世界观更新 | Peri 核心 |
| L2 | 递归分形 | Peri 专属（已实现，见合同 22） |

### 3.2 参与者边界（通用）

以下参与者边界规则适用于所有三种模式。唯一差异：Peri 的子 Agent 会主动"上报分歧"，而 MOA 的参考模型只输出提案（不主动上报），Default 的 Subagent 执行指令（不主动上报）。

| 参与者 | 做 | 不做 |
| :--- | :--- | :--- |
| 用户 | 输入意图、观察、打断 | 直接指挥子 Agent |
| 主 Agent | 翻译意图、守护世界观、矛盾路由 | 陷入局部细节 |
| 子 Agent | 自由探索、上报分歧（Peri 特有） | 直接对用户说话、永久占用 |

### 3.3 终止条件（通用 + Peri 专属）

层 0 和层 3 适用于所有模式；层 1 和层 2 是 **Peri 专属**（已实现）。

| 层级 | 定义 | 归属 |
| :--- | :--- | :--- |
| 层 0 | 预算耗尽 → 强制返回 | **通用** |
| 层 1 | SPRT 越界 → 统计收敛 | Peri 专属（已实现） |
| 层 2 | 分歧消解为表达性 → 认知低熵 | Peri 专属（已实现） |
| 层 3 | 用户新输入 → 重新开始 | **通用** |

**熵预算的工程化注释**：
- `max_tokens`：系统的"总质能"。MOA 消耗快但产生高价值负熵；基础/Peri 要求高效压缩。
- `max_wall_seconds`："物理宇宙光锥"。超限必须强制终态。

---

## 四、可观测性与负熵度量（Peri 专属愿景，部分已实现）

每种认知形态都会留下完整的运行轨迹。以下度量能力是 **Peri 模式的专属愿景**，当前已实现基础数据采集（run_events + artifacts）与收敛检测（SPRT/语义坍缩，见合同 18），但完整的语义距离与迭代质量分析尚未落地。

- **语义距离追踪**：追踪答案在语义空间中的漂移轨迹，识别"认知相位跃迁"与"空转"。【未实现】
- **迭代改进幅度**：量化 Peri 每轮迭代的质量增量，检测边际递减。【未实现】
- **共识熵**：衡量 MOA 各参考模型提案的多样性，判断聚合是否有效。【可跨模式复用】

这些度量工具的底层数据（run_events、artifacts、SQLite ledger）已就位，上层分析算法为后续方向。

---

## 五、工程约束

### 5.1 递归分形结构（Peri 专属）
每个 Agent 节点执行相同接口：
```
输入：世界观 W + 任务 T + 当前层级 L
输出：结果 R + 分歧报告 D + 层级转移建议 S
```
此结构是 Peri "分形生长宇宙"的工程基础。`convergence.max_recursion_depth` 开启后，Worker 通过 `spawn_subtask` 工具把自己的范围拆给子 Worker——深度受限、`_subtasks/` 子目录隔离、共享 run budget、`parent_task_id` 嵌套记账（见合同 22）。

### 5.2 子 Agent 池结构（通用）
- 主 Agent 需要时 `spawn(W₁, T₁)` → 子 Agent 化生。
- 子 Agent 收敛后 → 消亡。
- Default 的 Subagent、MOA 的 Reference、Peri 的 Worker 均遵循此生命周期。

### 5.3 分歧上报协议（Peri 专属）
子 Agent 发现自己约束不足时，必须上报：
```json
{
  "type": "contradiction",
  "层级": L,
  "问题描述": "...",
  "建议方案": "..."
}
```
此协议是 Peri 模式的核心机制，MOA 和 Default 不使用。

---

## 六、反面模式

1. 主 Agent 绕过审计直接写代码。
2. 仅靠固定轮次判断质量。
3. 用户直接指挥子 Agent。
4. 子 Agent 永久存活。
5. 主 Agent 微管子 Agent 每一步。

---

## 七、自检清单

- [ ] 这个设计违反了公理中的哪一条？
- [ ] 这个决策是否增加了"主 Agent 管得太多"的风险？
- [ ] 子 Agent 是否有足够的自由度？
- [ ] 用户能否在任何时候打断并重新定向？
- [ ] 矛盾检测是基于可观测量还是 Agent 自评？
- [ ] **本次改动是增加了"认知熵增"还是"负熵"？**
- [ ] **本次改动的成本结构是否明确？**
- [ ] **本次改动是否混淆了通用能力与 Peri 专属愿景？**

---

## 附录：架构元素归属速查表

| 架构元素 | Default | MOA | Peri |
| :--- | :--- | :--- | :--- |
| 参与者边界（3.2） | ✅ 通用 | ✅ 通用 | ✅ 通用 |
| 预算耗尽终态（3.3 层0） | ✅ | ✅ | ✅ |
| 用户打断终态（3.3 层3） | ✅ | ✅ | ✅ |
| 收敛检测维度（3.1） | ❌ | ❌ | ✅ **专属** |
| 矛盾路由维度（3.1） | ❌ | ❌ | ✅ **专属** |
| SPRT/语义收敛终态（3.3 层1/2） | ❌ | ❌ | ✅ **专属** |
| 分歧上报协议（5.3） | ❌ | ❌ | ✅ **专属** |
| 递归分形结构（5.1） | ❌ | ❌ | ✅ **专属** |
| 语义距离追踪（第四章） | ❌ | ⚠️ 可复用 | ✅ **专属愿景** |

---

文档已完整整合归属关系。主要变更：
1. **第三章**每小节顶部增加了归属指引。
2. **3.1/3.2/3.3** 内部用表格明确标注每项归属。
3. **第四章**明确标注哪些是 Peri 专属愿景、哪些已实现。
4. **第五章**区分通用约束与 Peri 专属协议。
5. **新增附录**：架构元素归属速查表。
6. **自检清单**增加一条归属混淆检查。
7. **"尚未实现"列表**明确标注 Peri 专属愿景。