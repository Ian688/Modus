{
  "name": "modus-desktop",
  "version": "0.1.0",
  "description": "Modus Desktop - multi-mode AI Agent",
  "main": "main.js",
  "scripts": {
    "start": "./node_modules/.bin/electron .",
    "serve": "cd .. && uv run modus serve --port 3000",
    "desktop": "./node_modules/.bin/electron ."
  },
  "devDependencies": {
    "electron": "^32.3.3"
  }
}
